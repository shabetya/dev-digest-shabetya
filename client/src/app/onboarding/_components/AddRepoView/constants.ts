/** Constants for AddRepoView. */

/** Outer page padding around the centered card. */
export const PAGE_PADDING = "44px 28px";

/** Card width (caps at 100% on narrow viewports). */
export const CARD_WIDTH = 520;

/** Logo mark box size + its icon size. */
export const LOGO_SIZE = 30;
export const LOGO_ICON_SIZE = 17;
